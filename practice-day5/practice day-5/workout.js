function workout(name) {
    var bicepworkout = ["simal", "komla", "fruit"];
    var chest = ["mans", "ja", "ah"];

    if (name == "bicep") {
        return bicepworkout;
    }
    else {
        return "dont know";
    }
}
var chestrexercie = workout("moov");
console.log(chestrexercie);